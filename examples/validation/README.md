# Validation Examples

- `invalid-review-queue.json` is a deliberately broken manifest.
- `invalid-review-queue.txt` is the checked-in expected output from `new-alphabet validate invalid-review-queue.json`.
- For passing manifests, use `../greenfield-blog/new-alphabet.json` and `../greenfield-workspace/new-alphabet.json`.
