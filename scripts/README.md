# Scripts

These scripts refresh and verify the public New Alphabet artifact set.

- `validate-workspace.sh` runs `cargo test --workspace`
- `build-demo-sites.sh` rebuilds the docs, demo-blog, and demo-saas static outputs
- `export-public-artifacts.sh` refreshes the checked-in schema bundle and split exports
